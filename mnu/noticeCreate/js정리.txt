let res = await axios.post(`${Url}/actual-upload`, { fileName: fileName });
${Url}/actual-upload: 업로드를 처리할 서버의 엔드포인트
{ fileName: fileName }: 업로드할 파일의 이름을 서버에 전달
서버의 응답을 res에 저장


multiUploadArray: 각 청크의 정보를 저장할 배열
for (let uploadCount = 1 ~ ): 청크 수 만큼 반복 / uploadCount: 현재 업로드 중인 청크 번호
let start ~: 청크 시작 위치 계산
let end ~: 청크 끝 위치 계산
let fileBlob ~: 현재 청크 파일조각 가져오기(마지막인 경우 파일의 끝까지 가져옴)

~~

let EtagHeader~: 얻은 ETag와 업로드한 청크의 번호로 객체 만들고 배열에 추가
 # ETag: http 프로토콜에서 사용하는 엔터티 태그의 약자
         특정 리소스(파일 or 데이터)의 특정 버전에 대한 식별자 역할